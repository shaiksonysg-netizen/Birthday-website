<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To My Dearest Husband ❤️</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&family=Playfair+Display:ital,wght@0,700;1,700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-dark: #080808;
            --accent: #ff4d6d;
            --glass: rgba(255, 255, 255, 0.08);
            --gold: #ffb703;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; outline: none; }

        body {
            background-color: var(--bg-dark);
            color: #ffffff;
            font-family: 'Inter', sans-serif;
            overflow-x: hidden;
            scroll-behavior: smooth;
        }

        /* Ambient background glow */
        .glow-bg {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: radial-gradient(circle at 20% 30%, #2a0a10 0%, #080808 100%);
            z-index: -2;
        }

        /* Music Floating Icon */
        #music-container {
            position: fixed;
            top: 25px;
            right: 25px;
            z-index: 2000;
            cursor: pointer;
            background: var(--glass);
            padding: 10px 20px;
            border-radius: 40px;
            border: 1px solid rgba(255,255,255,0.2);
            backdrop-filter: blur(10px);
            display: flex;
            align-items: center;
            gap: 10px;
            transition: 0.3s;
        }

        /* Photo Frame with Heart Pulse */
        .hero-img-container {
            position: relative;
            width: 220px; height: 220px;
            margin: 0 auto 30px;
        }

        .hero-img-container img {
            width: 100%; height: 100%;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid var(--accent);
            position: relative; z-index: 5;
        }

        .heart-pulse {
            position: absolute;
            top: -10px; left: -10px; right: -10px; bottom: -10px;
            border: 2px solid var(--accent);
            border-radius: 50%;
            animation: pulse-ring 2s infinite;
            z-index: 1;
        }

        @keyframes pulse-ring {
            0% { transform: scale(0.8); opacity: 1; }
            100% { transform: scale(1.3); opacity: 0; }
        }

        /* Modern Sections */
        section {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 40px 20px;
        }

        .glass-card {
            background: var(--glass);
            backdrop-filter: blur(25px);
            border: 1px solid rgba(255,255,255,0.1);
            border-radius: 35px;
            padding: 50px 30px;
            max-width: 550px;
            width: 100%;
            text-align: center;
            box-shadow: 0 30px 60px rgba(0,0,0,0.6);
            opacity: 0; transform: translateY(40px);
            transition: 1.2s all ease-out;
        }

        .glass-card.show { opacity: 1; transform: translateY(0); }

        h1 {
            font-family: 'Playfair Display', serif;
            font-size: 3.2rem;
            margin-bottom: 20px;
            color: var(--accent);
        }

        .sweet-note {
            font-size: 1.15rem;
            line-height: 1.8;
            color: #ececec;
            font-style: italic;
        }

        .btn-modern {
            margin-top: 35px;
            padding: 15px 40px;
            background: var(--accent);
            color: white;
            border: none;
            border-radius: 50px;
            font-weight: 600;
            cursor: pointer;
            box-shadow: 0 10px 25px rgba(255, 77, 109, 0.4);
            transition: 0.3s;
        }

        .btn-modern:hover { transform: scale(1.05); }

        /* Floating Symbols */
        .symbol {
            position: absolute;
            pointer-events: none;
            opacity: 0.4;
            animation: float-around 6s infinite ease-in-out;
        }

        @keyframes float-around {
            0%, 100% { transform: translate(0,0); }
            50% { transform: translate(20px, -30px); }
        }
    </style>
</head>
<body>

    <div class="glow-bg"></div>

    <audio id="love-music" loop>
        <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-8.mp3" type="audio/mpeg">
    </audio>

    <div id="music-container" onclick="handleMusic()">
        <span id="m-icon">🎵</span> <span id="m-status">Play Vibes</span>
    </div>

    <section>
        <div class="glass-card" id="card1">
            <div class="hero-img-container">
                <div class="heart-pulse"></div>
                <img src="https://images.unsplash.com/photo-1506794778202-cad84cf45f1d?auto=format&fit=crop&w=400&q=80" alt="Husband">
            </div>
            <h1>Happy Birthday, My King 👑</h1>
            <p class="sweet-note">In the story of my life, you are the best chapter. Today, the world celebrates you, but my heart celebrates you every single day.</p>
            <button class="btn-modern" onclick="goNext('card2')">Feel the Love</button>
        </div>
    </section>

    <section>
        <div class="glass-card" id="card2">
            <h1>2026 & Always ❤️</h1>
            <p class="sweet-note">"To the man who handles my moods and makes my dreams come true. Thank you for being the world's best husband. May your day be as handsome as your smile!"</p>
            <div style="font-size: 50px; margin-top: 20px;">🌹✨</div>
            <button class="btn-modern" onclick="triggerHearts()">Click for Magic ✨</button>
        </div>
    </section>

    <script>
        const song = document.getElementById('love-music');
        function handleMusic() {
            if(song.paused) {
                song.play();
                document.getElementById('m-icon').innerHTML = '⏸️';
                document.getElementById('m-status').innerHTML = 'Playing';
            } else {
                song.pause();
                document.getElementById('m-icon').innerHTML = '🎵';
                document.getElementById('m-status').innerHTML = 'Paused';
            }
        }

        function goNext(id) {
            document.getElementById(id).scrollIntoView({ behavior: 'smooth' });
        }

        // Scroll Reveal Effect
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(e => { if(e.isIntersecting) e.target.classList.add('show'); });
        }, { threshold: 0.3 });

        document.querySelectorAll('.glass-card').forEach(c => observer.observe(c));

        // Magic Heart Rain
        function triggerHearts() {
            for(let i=0; i<80; i++) {
                setTimeout(() => {
                    const h = document.createElement('div');
                    h.innerHTML = ['❤️','💖','✨','🥂'][Math.floor(Math.random()*4)];
                    h.style.position = 'fixed';
                    h.style.left = Math.random() * 100 + 'vw';
                    h.style.top = '-10vh';
                    h.style.fontSize = Math.random() * 30 + 15 + 'px';
                    h.style.zIndex = '3000';
                    h.style.transition = '4s linear';
                    document.body.appendChild(h);
                    
                    setTimeout(() => {
                        h.style.transform = `translateY(110vh) rotate(${Math.random()*360}deg)`;
                        h.style.opacity = '0';
                    }, 100);
                    setTimeout(() => h.remove(), 5000);
                }, i * 60);
            }
        }
    </script>
</body>
</html>